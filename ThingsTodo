1. The image should be only AxB pixels; or we can change that with CSS
2. The Signin should be pre authenticated by a google account
3. Make the feed page; add like, post and comment
4. Make like, post, Comment all in one page; since it would make life easier
5. Id say 80% it; can work on it later in the break or smt but for now 80% is good.
6. do css last

Ideally I want to the feed to have a post button and right underneath the user can see 
their post. It would be like photo then text. If its only photo, or only text then its only that. It doesnt force users to upload both like the profile naming does.

Each post right below it will have a comment, like button. When pressed like, the like button will change the count from 0 to 1. Only 1 like per acc. when pressed comment the user will see their comment pop up beneath the post beneath the comment button.

eventually try making it more user friendly, more like insta